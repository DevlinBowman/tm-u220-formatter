#!/bin/sh
# Opens the checkout-only browser tool directly or behind `220 dev glyphs`.
# Direct invocation retains advanced receipt and loopback-server options.
set -eu

script_dir=$(CDPATH= cd -P "$(dirname "$0")" && pwd)
exec node "$script_dir/glyph_editor/server/main.mjs" "$@"
